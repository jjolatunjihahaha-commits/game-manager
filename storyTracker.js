/**
 * Very lightweight timeline tracker. Append events to chatMetadata so they persist with the chat.
 */

export function trackEvent(message, chat, globalState) {
  // TODO: entity extraction, timeline updates, trigger pendingCardUpdate / pendingLoreUpdate.
}
