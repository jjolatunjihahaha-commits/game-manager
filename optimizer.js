/**
 * Prompt optimiser – dedupe, prune, compress, reorder.
 * Stubbed with a simple no-op pass-through. Implement your logic inside optimisePrompt().
 */

export async function optimisePrompt(rawPrompt, { tokenCap = 3500 } = {}) {
  // TODO: real optimisation logic.
  // For now, just return rawPrompt unchanged.
  return rawPrompt;
}
